# TODO - Game of Life

---

- Mobile ColorPicker - Cannot type hex code easily. Maybe center hex code in input too.
- Mobile Horizontal mode needs work.
- SVG Icons - Normalize all to pattern used in `HelpIcon.tsx`.
